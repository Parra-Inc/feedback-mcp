# Local development database only. To self-host the full app with Docker,
# use the compose files at the repository root.
name: feedback-mcp-dev

services:
  postgres:
    image: postgres:17
    ports:
      - "5452:5432"
    environment:
      POSTGRES_USER: feedback
      POSTGRES_PASSWORD: feedback
      POSTGRES_DB: feedback
    volumes:
      - feedback-mcp-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U feedback -d feedback"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  feedback-mcp-postgres:
